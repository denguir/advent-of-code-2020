#include <vector>
#include <tuple>

int dico_search(int, std::vector<int>, int, int);
std::tuple<int, int, int> pair_search(int, std::vector<int>);
std::tuple<int, int, int, int> triplet_search(int, std::vector<int>);